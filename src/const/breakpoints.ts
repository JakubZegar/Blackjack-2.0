import { TBreakpoints } from "../types/global"

const laptop = '1280px';
const tablet = '1024px';
const mobile = '600px';

export const breakpoints: TBreakpoints = {
    laptop: laptop,
    tablet: tablet,
    mobile: mobile
}